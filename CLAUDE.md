# CLAUDE.md

Guidance for Claude Code when working in this repository.

See `README.md` for the project overview.

## Agent skills

### Issue tracker

GitHub issues at `github.com/vitorpaixao/lingua` via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context layout — `CONTEXT-MAP.md` at root pointing at per-area `CONTEXT.md` files under `orchestrator/`, `web/`, `docker/`. See `docs/agents/domain.md`.
